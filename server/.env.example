# MongoDB connection string
# For MongoDB Atlas, format is:
# mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority&appName=<your-app>
MONGODB_URI=

# Optional explicit DB name (if not embedded in URI)
# MONGODB_DB=wli

# Server port (optional)
# PORT=4000
